<%= simple_form_for([ item, review ], remote: true) do |form| %>
  <%= form.input :content, as: :text, label: false, placeholder: 'Tell us what you think :)' %>
  <%= form.button :submit %>
<% end %>
