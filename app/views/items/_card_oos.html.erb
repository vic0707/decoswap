<div class="hovereffect">
  <div class="card"
    style="background-image: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.3)), url('<%= cl_image_path item.photos.first.path if item.photos.first %>')">
        <div class="overlay">
          <h2>Object rented...</h2>
          <%= link_to "See when it's available", item_path(item), class: "info" %>
        </div>
        <div class="card-description">
          <div class="text-center"><%= item.name %> </div>
            <p><%= item.category_item %></p>
            <p><%= item.price %> €/month</p>
          </div>
  </div>
</div>
