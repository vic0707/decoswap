<div class="card-category text-center">
	<h2><%= @item.name %></h2>
	<p><%= @item.category_item %></p>
	<p><%= @item.description %></p>
	<p><%= @item.price %></p>
</div>
</div>

<%= link_to "Book", new_item_booking_path(@item), class: "btn btn-primary center" %>
