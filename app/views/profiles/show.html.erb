<ul>
	<% @user.bookings.each do |booking| %>
	<li>
		<%= booking.item.name %> - <%= booking.item.size %> - <%= booking.item.status %>

	</li>
	<% end %>
</ul>

<%= link_to "Edit Profile", edit_profile_path, class: "btn btn-primary center" %>

